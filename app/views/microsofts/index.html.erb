<h1>Listing microsofts</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @microsofts.each do |microsoft| %>
      <tr>
        <td><%= microsoft.content %></td>
        <td><%= microsoft.user_id %></td>
        <td><%= link_to 'Show', microsoft %></td>
        <td><%= link_to 'Edit', edit_microsoft_path(microsoft) %></td>
        <td><%= link_to 'Destroy', microsoft, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Microsoft', new_microsoft_path %>
